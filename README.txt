Project for CAP4630-2018 Fall Semester

Compile into an exectuable jar with weka.jar in a folder named "lib" in the same directory as the jar.

To Run:
java -jar MulticlassPerceptron.jar <FILE_NAME> <EPOCHS_TO_TRAIN_FOR>